import { AbstractLoadStrategy, ILoadConfig } from './AbstractLoadStrategy';
export interface IMediaElementLoadConfig extends ILoadConfig {
    sourceSet?: string[];
    mimeTypes?: string[];
    loadElement?: HTMLMediaElement;
}
export declare abstract class MediaElementLoadStrategy extends AbstractLoadStrategy<IMediaElementLoadConfig> {
    readonly elementType: ('audio' | 'video');
    private _boundOnLoad;
    private _boundOnError;
    private _boundOnTimeout;
    private _element;
    private _elementTimer;
    constructor(config: IMediaElementLoadConfig, elementType: ('audio' | 'video'));
    load(): void;
    abort(): void;
    private _createElement;
    private _clearEvents;
    private _error;
    private _complete;
    private _onLoad;
    private _onError;
    private _onTimeout;
}
