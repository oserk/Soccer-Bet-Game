import { AbstractLoadStrategy, ILoadConfig } from './AbstractLoadStrategy';
export interface IImageLoadConfig extends ILoadConfig {
    loadElement?: HTMLImageElement;
}
export declare class ImageLoadStrategy extends AbstractLoadStrategy<IImageLoadConfig> {
    private _boundOnLoad;
    private _boundOnError;
    private _boundOnTimeout;
    private _element;
    private _elementTimer;
    load(): void;
    abort(): void;
    private _createElement;
    private _clearEvents;
    private _error;
    private _complete;
    private _onLoad;
    private _onError;
    private _onTimeout;
}
